# Laravel API origin for production builds (npm run build / npm run preview).
# Use the full backend URL when the SPA is served separately from the API.
VITE_API_BASE_URL=http://127.0.0.1:8000

# For local dev (npm run dev), see .env.development — it uses /api via the Vite proxy.
